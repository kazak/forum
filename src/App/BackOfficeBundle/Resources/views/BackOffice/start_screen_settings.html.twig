{% extends "AppBackOfficeBundle::pagelayout.html.twig" %}

{% block content %}
    <!-- Main jumbotron for a primary marketing message or call to action -->
    <div class="jumbotron">
        <div class="container">
            <h1>Close the web ordering</h1>
            <p>If we experience problems we may want to stop customers from sending us orders from the web site. When we stop the customer we display a message that informs him that the ordering is closed. Below you will find a form to close and reopen the web ordering. Here you must also enter the title and message that should be presented to the customer.</p>
        </div>
    </div>

    <div class="container">
        <!-- Example row of columns -->
        <div class="row">
            <div class="col-md-12">

                <!-- Show error message if title or message is empty and the user tries to close the ordering -->
                <div class="alert alert-warning" role="alert"
                     data-content="{% if params.message %}true{% else %}false{% endif %}">
                    {% if params.message %}{{ params.message }}{% else %}no set message{% endif %}</div>

                {% if params.state == false %}
                    <!-- Show this message after the ordering has been closed -->
                    <div class="alert alert-success" role="alert">The web ordering is now closed!</div>
                {% else %}
                    <!-- Show this message after the ordering has been reopened -->
                    <div class="alert alert-success" role="alert">The web ordering is now reopened!</div>
                {% endif %}
                <form action="{{ path('app_back_office_state_update') }}" id="update-status" method="post">
                    <div class="form-group">
                        <label for="message">Message:</label>
                        <input type="text" class="form-control" name="message" id="message" autocomplete="off"/>
                    </div>
                    <div class="form-group">
                            <!-- Show when web ordering is open -->
                        <input type="button" class="btn btn-lg" id="update-message" value="Update the message"
                               disabled autocomplete="off"/>
                        <input type="hidden" id="close-web" name="status" value="1" >
                            <!-- Show these two buttons when the ordering is closed -->
                        {% if params.state == false %}
                            <input type="submit" class="btn btn-lg" value="Reopen the ordering"
                                   {% if params.message %}{{ params.message }}{% else %}disabled{% endif %}
                                   autocomplete="off"/>
                        {% else %}
                            <input type="submit" class="btn btn-lg" value="Close the ordering"
                                   autocomplete="off"
                                   {% if params.message %}{{ params.message }}{% else %}disabled{% endif %}/>
                        {% endif %}
                    </div>
                </form>
            </div>
        </div>
    </div>
    {% javascripts
    'bundles/appbackoffice/js/web_state.js'
    %}
    <script src="{{ asset_url }}"></script>
    {% endjavascripts %}
{% endblock %}
